name: magic_logger
description: "Demo artisan plugin, file-tailed logger with install + tail + level + uninstall commands. Lives under example/packages/ to demonstrate the third-party plugin pattern end-to-end."
version: 0.0.1-alpha.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  fluttersdk_artisan:
    path: ../../../
  meta: ^1.15.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
