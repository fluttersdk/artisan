/// Runtime API for awesome_plugin.
///
/// Plugin authors expose their public runtime surface here. Consumers import
/// via `package:awesome_plugin/awesome_plugin.dart` to gain access to the runtime
/// classes (controllers, services, models), the CLI surface lives in
/// `package:awesome_plugin/cli.dart` and is invoked through `artisan` only.
library;

// TODO: export your runtime classes.
