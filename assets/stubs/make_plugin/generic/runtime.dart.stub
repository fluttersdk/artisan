/// Runtime API for {{ name }}.
///
/// Plugin authors expose their public runtime surface here. Consumers import
/// via `package:{{ name }}/{{ name }}.dart` to gain access to the runtime
/// classes (controllers, services, models), the CLI surface lives in
/// `package:{{ name }}/cli.dart` and is invoked through `artisan` only.
library;

// TODO: export your runtime classes.
