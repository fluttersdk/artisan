<!-- Generated by ac:claude-md-rules-creator on 2026-05-19. -->

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Stack

Pure Dart 3.4+ CLI framework. NO Flutter runtime dependency: the package is consumed by Flutter apps via `dart run fluttersdk_artisan <cmd>` but the framework itself runs on the Dart VM. Vendor deps locked: `args:^2.7`, `vm_service:^15.2` (DDS-aware), `dart_mcp:^0.5.1` (labs.dart.dev official MCP SDK), `stream_channel:^2.1` (dart_mcp transport surface), `xml`, `yaml`, `yaml_edit:^2.2.3` (in-place YAML mutation), `crypto`, `meta`, `path`.

## Commands

| Command | When |
|---|---|
| `dart test` | Run all tests (~1070 baseline). |
| `dart format lib/ test/ bin/` | Format. Must produce no diff. |
| `dart analyze` | Static analysis. Zero issues required across `lib/ test/ bin/`. |
| `dart run fluttersdk_artisan <cmd>` | Run a built-in command standalone (no consumer wrapper needed). |
| `dart run fluttersdk_artisan:mcp` | Stdio JSON-RPC MCP server entry. Reads `~/.artisan/state.json` for VM Service URI; soft-fails with 0 plugin tools when no Flutter app is running. |
| `dart pub publish --dry-run` | Validate publish archive. Target under 500 KB compressed (current ~277 KB after `.pubignore`). |

## Architecture

Single barrel: `package:fluttersdk_artisan/artisan.dart` re-exports the full public API. `lib/fluttersdk_artisan.dart` is a convention sibling that re-exports the same. Two binaries under `bin/`: `fluttersdk_artisan.dart` (CLI) + `mcp.dart` (MCP server entry; prepends `mcp:serve`, forces `collectMcpTools: true`, forces `delegateToConsumer: false` so the substrate's complete builtin list owns dispatch). Subsystem-first layout under `lib/src/`:

| Path | Purpose |
|---|---|
| `console/` | `ArtisanApplication` + `ArtisanRegistry` + `CommandSignature` (Laravel-style DSL) + `ArtisanContext`/`Input`/`Output`. `runArtisan(args, collectMcpTools:, delegateToConsumer:)` is the shared entry. |
| `commands/` | Built-in commands. Naming: `<Verb>Command extends ArtisanCommand`. Includes `mcp:serve` / `mcp:install` / `mcp:uninstall` alongside the CLI surface. |
| `installer/` | `PluginInstaller` fluent DSL + `ManifestInstaller` + `InstallTransaction` + `PluginsRegistryFile` + sealed `InstallOperation` hierarchy. See `.claude/rules/installer.md`. |
| `mcp/` | `McpServer extends MCPServer with ToolsSupport` (dart_mcp) + `McpToolDescriptor` + `McpFilterConfig` (3-layer: file + env + CLI, Cargo-style replace + deny-wins) + `ArtisanMcpToolCollisionException`. Substrate commands surface as `artisan_*` MCP tools via `_artisanCommandTools()` (9 allowlisted: start/stop/status/logs/restart/reload/hot-restart/doctor/list). |
| `helpers/` | `FileHelper`, `ConfigEditor` (idempotent inject), `MainDartEditor`, `EnvEditor`, `PlistWriter`, `StringHelper`. |
| `stubs/` | `StubLoader` (asset bundle + filesystem resolution). Stub assets live in `assets/stubs/`. |
| `state/` | `StateFile` (JSON persistence for `start`/`stop`/`status`/`mcp:serve` discovery). |
| `tinker/`, `vm/` | REPL + `VmServiceClient` (wraps `package:vm_service`, no isolate-id cache). |

## Conventions

- **Signature DSL primary**: command declaration via `String get signature => 'cmd:name {arg} {--flag}'`. `configure(ArgParser)` is the explicit fallback for cases the DSL cannot express.
- **`final class` universal** on every new public type (`McpServer`, `McpToolDescriptor`, `McpFilterConfig`, etc.). Sealed dispatch via Dart 3 exhaustiveness (no `default:` branch on sealed switches).
- **Plugin MCP contract**: `ArtisanServiceProvider.mcpTools() => const <McpToolDescriptor>[]` default-empty. Plugins override to expose tools; the MCP server collects them when `runArtisan(collectMcpTools: true)`.
- **MCP tool descriptions** follow Claude Code canonical format: imperative opening sentence, brief context paragraph, `Usage:` bullet list, constraint-forward language. Per-property `inputSchema` descriptions include defaults + concrete examples. Critical info first (CC truncates MCP descriptions at 2KB chars).
- **Idempotent installers**: `ConfigEditor.insertCodeAfterPattern` early-returns when `content.contains(code.trim())`; `PluginInstaller` injects via lookahead-anchored regex (`(?=\s*\n\s*\])`) so re-running `plugin:install` is a safe no-op.
- **Atomic writes**: every persistent file write goes through `.tmp` + rename (`PluginsRegistryFile`, `InstallTransaction`, `PluginsRefreshCommand`). Concurrent readers never see partial state.
- **Codegen barrels**: `lib/app/commands/_index.g.dart` (consumer commands) + `lib/app/_plugins.g.dart` (plugin providers). Both are GENERATED files. Never hand-edit. Regenerated by `commands:refresh` / `plugins:refresh` / `make:command` / `plugin:install`.
- **Stub placeholders**: `{{ name }}`, `{{ pascalName }}`, `{{ commandPrefix }}` resolved at scaffold time via `StubLoader.replace`. Stub file name keeps `.stub` suffix; rendered target gets the real extension.
- **Numbered step comments** (`// 1.` `// 2.`) for methods with 3+ sequential phases. Docblocks per public class/method explaining WHY + invariants. Internal comments are plain `// `; only public API gets `///`.

## Off-limits

- `example/` and `example_magic/` are dev playgrounds for live e2e validation. Production code does not depend on either. `example_magic/` ships its own `magic` + `my_plugin` baseline for the Magic-managed flow; `example/` is the Magic-less native path.
- `*.g.dart` files (`_index.g.dart`, `_plugins.g.dart`) are codegen output. Edit the source of truth (`.artisan/plugins.json` for plugins; `lib/app/commands/<name>_command.dart` for commands) and re-run the matching refresh command.
- `doc/install_yaml_schema.md` + `doc/plugin_authoring_guide.md` are user-facing references. Keep in sync with `lib/src/installer/install_manifest.dart` + `manifest_parser.dart` whenever the schema changes.
- `.pubignore` excludes `example_magic/`, `example/{android,ios,macos,linux,windows,web}/`, `build/`, `coverage/`, `.idea/`, `.artisan/`, `.ac/` from the pub.dev archive. Adding new top-level dirs that should not ship: extend `.pubignore`.

## Release

- Version: 0.0.1 (first public release). Pre-release: bump per SemVer in `pubspec.yaml`, add the entry to `CHANGELOG.md` under `[Unreleased]` then promote to a dated section.
- Topics (pub.dev search): `cli`, `artisan`, `scaffolding`, `plugin-system`, `code-generation` (max 5; declared in `pubspec.yaml`).
- `dart pub publish --dry-run` must end clean (0 errors). Dev-state warnings (uncommitted / gitignored-but-tracked) are acceptable in dev but cleared before publish via clean git checkout.
- No CI workflow; releases are manual. Run `dart test` + `dart analyze` + `dart pub publish --dry-run` before tagging.
